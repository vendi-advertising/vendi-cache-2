parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        securitychecker: ~
        git_blacklist:
            keywords:
                - 'die('
                - 'var_dump('
                - 'exit;'
        composer: ~
